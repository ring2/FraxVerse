# FraxVerse 碎片宇宙 · 统一待办清单

> ⏱ 最后更新：2026-05-03
> 代码规模：44 commits · 573 passed / 4 design-audit FAIL · 全栈已 Docker 化
> 当前评分：7.0/10（Mock 清除 + QueuePool 修复）
> 致命短板：**微信 iLink 推送不可用（ret=-2） + 0 笔真实交易记录**

---

## 阅读指引

| 等级 | 含义 |
|:----:|:------|
| 🔴 P0 | 阻塞级 — 不做系统不能用/风险大 |
| 🟠 P1 | 高优先级 — 短期内有实际收益 |
| 🟡 P2 | 中优先级 — 补充体验/完善功能 |
| 🟢 P3 | 低优先级 — 锦上添花 |

---

## 🟠 P1 — 高优先级

### P1-1 🪟 租 Win 云服务器 + 部署 quant-qmt-proxy（关键依赖）

> **为什么：** 这是上实盘的唯一路径。LIVE 模式代码已就绪（573 测试全过），
> 就差一台 Windows 机器跑 miniQMT + quant-qmt-proxy。

| # | 任务 | 预计时间 |
|:-:|:-----|:-------:|
| 1 | 租低配 Win 云服务器（阿里云/腾讯云 60-80元/月，2核2G） | 10min |
| 2 | 装 miniQMT + 登录、绑定券商账号 | 30min |
| 3 | 装 quant-qmt-proxy（https://github.com/liqimore/quant-qmt-proxy） | 15min |
| 4 | 配置 `.env` 的 `QMT_PROXY_URL` / `API_KEY` / `ACCOUNT_ID`（后端已就绪） | 5min |
| 5 | 切 LIVE 模式验证下单/查询链路（后端已就绪） | 15min |

**相关文件：** `src/execution/qmt_proxy_client.py` · `src/execution/live_broker.py` · `src/execution/engine.py` · `src/config.py`

---

### P1-2 ✅ 移动端 Mock 已全面清除（`cab44e6`）

> **变更：** 三个移动端页面所有 `MOCK_*` 常量 + 硬编码 fallback 值已删除，
> 改为真实 API 调用，空数据时展示 0 或空列表。

| 页面 | 改动 |
|:-----|:-----|
| **MobileTrade.tsx** | 删除 MOCK_POSITIONS/MOCK_ORDERS/MOCK_PORTFOLIO，`?? 985420`→`?? 0` 等 |
| **MobileDashboard.tsx** | 删除 MOCK_SUMMARY/MOCK_TRADE_MODE/MOCK_MARKET_STATE，`"1,284,350"`→`"0"` |
| **MobileStockPool.tsx** | 删除 MOCK_POOL，`.catch → setItems([])` |

---

### P1-4 ⚠️ 每日收盘推送 cron

> **现状：** cron 已建（`0 16 * * 1-5`，job_id: `3f387311e2da`），
> 但 **iLink 服务端从 4/30 起返回 ret=-2**（所有 sendmessage 被拒）。
> 接收（polling）正常，只能收不能发。非代码可修，需 iLink 侧解决。

| 文件 | 说明 |
|:-----|:------|
| `scripts/hermes_cron_push.py` | cron 调度入口脚本，调后端 API + 独立推送脚本 |
| `/home/ubuntu/hermes_weixin_push.py` | 独立 iLink 推送（requests 同步，绕开 aiohttp） |

---

## 🟡 P2 — 中优先级

### P2-1 🔄 完整回测跑一轮

| 任务 | 说明 | 预计时间 |
|:----|:-----|:-------:|
| 用 backtesting 库跑 2 年历史数据 | 跑完两个策略 | 1h |
| 输出回测报告 | 收益率/最大回撤/夏普率/胜率 | 30min |
| 参数敏感性分析 | 评分阈值、止盈阶梯、分批比例 | 1h |

---

### P2-2 ✅ Docker Compose 完善

> **已完成：** 后端 FastAPI + 前端 Nginx + Postgres + Redis 全栈容器化。
> apt/pip 走腾讯云内网镜像，build 时间 < 3min。前端分包优化（入口 652KB → 21KB）。

4 个服务（postgres + redis + backend + frontend），外网 `:3000` 可访问。

---

### P2-3 ✅ Alembic 迁移体系

> **已完成：** `alembic init` + 初始迁移（27 张业务表 + alembic_version）。
> 设计审计测试未跟进（预期 35 张表，现 27 张——旧表被迁移清理）。

---

### P2-4 🔗 GitHub CI

| 任务 | 预计时间 |
|:-----|:-------:|
| CI 自动跑测试（.github/workflows） | 20min |
| 自动生成 API 类型定义（gen:api） | 15min |

---

## 🟢 P3 — 低优先级

### P3-1 📊 数据分析与复盘

- [ ] 决策回溯面板（历史决策 → 实际涨跌 → 胜率）
- [ ] 策略分组对比
- [ ] 交易日志分析

### P3-2 🎨 UX 打磨

- [ ] Dashboard 数据可视化
- [ ] 操作快捷入口
- [ ] 通知中心完善

### P3-3 📖 README

- [ ] 部署说明 / 环境要求 / 配置指南 / 开发流程

### P3-4 🧪 后端缺 endpoint

| 端点 | 问题 |
|:-----|:-----|
| `GET /market/market-state` 返回 list | 前端只取第一项，不影响 |
| `StockPool` 细分指标字段 | 前端 metrics 此前是 mock 生成，已随 Mock 清除改为空 |

---

## 已知问题

| # | 问题 | 严重度 | 状态 |
|:-:|:-----|:------:|:----:|
| 1 | iLink sendmessage 返回 ret=-2（4/30起），只能收不能发 | 🔴 | 待 iLink 侧修复 |
| 2 | 设计审计测试 4 个 FAIL（预期 35 表 vs 实际 27 表） | 🟢 | 审计测试未跟进 Alembic 迁移 |
| 3 | DB QueuePool 耗尽（pool_size=5→10 已修复 `b82248a`） | ✅ | 已修复 |
| 4 | FastAPIDeprecationWarning（`regex`→`pattern`） | 🟢 | 兼容性，不影响运行 |
| 5 | 磁盘 42G/59G（74%） | 🟡 | 需留意，短期无风险 |

---

## 进度总览

| 阶段 | 状态 |
|:-----|:----:|
| P0 纯规则引擎 | ✅ 16/16 100% |
| P1 基础设施+前后端 | ✅ ~90%（Mock 清除+Pool 修复完成，微信推送卡 iLink） |
| P2 AI Agent | ✅ 11/11 100% |
| P2 运维（Docker+Alembic） | ✅ 已完成 |
| P2-4 GitHub CI | ⬜ 未开始 |
| P3 自动进化 | ❌ 0/3 未开始 |
