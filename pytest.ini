[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: 单元测试, 需要快速执行
    integration: 集成测试, 可能依赖外部服务
    backtest: 策略回测, 执行时间较长
    slow: 耗时测试（>30秒）
    regression: 回归测试
addopts = -v --strict-markers --tb=short -p no:cacheprovider --maxfail=5
